# Unitree H1 locomotion policy - Unity Sentis bundle

Exported from Isaac Lab task `Isaac-Velocity-Flat-H1-Play-v0` (checkpoint trained on `Isaac-Velocity-Flat-H1-v0`).
Everything here was generated by `source/export_h1_unity.py`; every number comes from the
live simulation, not from documentation.

## Contents

| Path | What it is |
|---|---|
| `h1_policy.onnx` | The actor network. opset 15, IR 8, single file, fixed batch 1. |
| `export_report.json` | Everything below, machine-readable. |
| `isaac_reference.json` | 250-step ground-truth trajectory of one robot (obs, action, root pose, joint state). |
| `checkpoint/` | Raw `checkpoint.pt` plus the resolved `env.yaml` / `agent.yaml`. |
| `robot/` | `h1.urdf` (vendor description) and `usd/` (the exact USD Isaac Lab simulated). |
| `source/` | Task source: env cfgs, MDP terms, robot asset cfg, and this exporter. |

## Policy interface

* **Input** `obs` - `float32[1, 69]`
* **Output** `actions` - `float32[1, 19]`
* Deterministic (the Gaussian policy's mean). No observation normalization
  (`obs_normalization=False`), so feed raw observations.
* Network: MLP [128, 128, 128], `elu` activations.
* Operators used: Elu, Gemm - all within Sentis' supported operator set.
* ONNX vs PyTorch over 250 real observations: **max abs diff 2.38e-06**.
* Run inference at **50.0 Hz** (20.0 ms per step).

## Observation layout (69 floats, concatenated in this order)

| Term | Index range | Size | Meaning |
|---|---|---|---|
| `base_lin_vel` | 0-2 | 3 | base linear velocity in the base frame [m/s] |
| `base_ang_vel` | 3-5 | 3 | base angular velocity in the base frame [rad/s] |
| `projected_gravity` | 6-8 | 3 | gravity unit vector rotated into the base frame (Isaac Z-up) |
| `velocity_commands` | 9-11 | 3 | user command: vx [m/s], vy [m/s], wz [rad/s] |
| `joint_pos` | 12-30 | 19 | joint positions **relative to the default pose** [rad] |
| `joint_vel` | 31-49 | 19 | joint velocities relative to default [rad/s] |
| `actions` | 50-68 | 19 | previous action vector (raw policy output) |

All observations are in **Isaac's Z-up right-handed** frame. Convert Unity state into that frame
*before* filling the vector (see the conversion section below).

## Actions to joint targets

The policy emits 19 numbers. They are **not** torques and **not** absolute angles:

```
joint_position_target[i] = default_joint_pos[i] + 0.5 * action[i]      # [rad]
```

An implicit PD drive then tracks that target every physics tick:

```
tau[i] = kp[i] * (joint_position_target[i] - q[i]) - kd[i] * qd[i]     # clamped to effort_limit[i]
```

### Joint order, defaults and limits

| # | Joint | default [rad] | lower [rad] | upper [rad] |
|---|---|---|---|---|
| 0 | `left_hip_yaw` | +0.0000 | -0.4300 | +0.4300 |
| 1 | `right_hip_yaw` | +0.0000 | -0.4300 | +0.4300 |
| 2 | `torso` | +0.0000 | -2.3500 | +2.3500 |
| 3 | `left_hip_roll` | +0.0000 | -0.4300 | +0.4300 |
| 4 | `right_hip_roll` | +0.0000 | -0.4300 | +0.4300 |
| 5 | `left_shoulder_pitch` | +0.2800 | -2.8700 | +2.8700 |
| 6 | `right_shoulder_pitch` | +0.2800 | -2.8700 | +2.8700 |
| 7 | `left_hip_pitch` | -0.2800 | -1.5700 | +1.5700 |
| 8 | `right_hip_pitch` | -0.2800 | -1.5700 | +1.5700 |
| 9 | `left_shoulder_roll` | +0.0000 | -0.3400 | +3.1100 |
| 10 | `right_shoulder_roll` | +0.0000 | -3.1100 | +0.3400 |
| 11 | `left_knee` | +0.7900 | -0.2600 | +2.0500 |
| 12 | `right_knee` | +0.7900 | -0.2600 | +2.0500 |
| 13 | `left_shoulder_yaw` | +0.0000 | -1.3000 | +4.4500 |
| 14 | `right_shoulder_yaw` | +0.0000 | -4.4500 | +1.3000 |
| 15 | `left_ankle` | -0.5200 | -0.8700 | +0.5200 |
| 16 | `right_ankle` | -0.5200 | -0.8700 | +0.5200 |
| 17 | `left_elbow` | +0.5200 | -1.2500 | +2.6100 |
| 18 | `right_elbow` | +0.5200 | -1.2500 | +2.6100 |

### PD drive settings

| Actuator group | Joints | Stiffness kp | Damping kd | Effort limit [N.m] |
|---|---|---|---|---|
| `legs` | `['.*_hip_yaw', '.*_hip_roll', '.*_hip_pitch', '.*_knee', 'torso']` | {'.*_hip_yaw': 150.0, '.*_hip_roll': 150.0, '.*_hip_pitch': 200.0, '.*_knee': 200.0, 'torso': 200.0} | {'.*_hip_yaw': 5.0, '.*_hip_roll': 5.0, '.*_hip_pitch': 5.0, '.*_knee': 5.0, 'torso': 5.0} | 300 |
| `feet` | `['.*_ankle']` | {'.*_ankle': 20.0} | {'.*_ankle': 4.0} | 100 |
| `arms` | `['.*_shoulder_pitch', '.*_shoulder_roll', '.*_shoulder_yaw', '.*_elbow']` | {'.*_shoulder_pitch': 40.0, '.*_shoulder_roll': 40.0, '.*_shoulder_yaw': 40.0, '.*_elbow': 40.0} | {'.*_shoulder_pitch': 10.0, '.*_shoulder_roll': 10.0, '.*_shoulder_yaw': 10.0, '.*_elbow': 10.0} | 300 |

Physics runs at **200 Hz** with decimation **4**, i.e. the PD drive
updates 4x per policy step. In Unity set `Time.fixedDeltaTime = 0.005000` and run
inference every 4 fixed steps. Unity's `ArticulationDrive` works in **degrees**: multiply
target radians by `Mathf.Rad2Deg`, and divide the kp/kd above by `Mathf.Rad2Deg` when porting the gains.

## Isaac (Z-up) to Unity (Y-up)

Isaac Lab is **right-handed, Z-up, X-forward**. Unity is **left-handed, Y-up, Z-forward**.

```csharp
// positions and linear velocities
Vector3 ToUnity(Vector3 isaac) => new Vector3(-isaac.y, isaac.z, isaac.x);
Vector3 ToIsaac(Vector3 unity) => new Vector3( unity.z, -unity.x, unity.y);

// quaternions are stored WXYZ in isaac_reference.json; Unity uses XYZW
Quaternion QuatToUnity(float w, float x, float y, float z) => new Quaternion(y, -z, -x, w);

// angular velocity flips sign with handedness
Vector3 AngVelToUnity(Vector3 isaac) => new Vector3(isaac.y, -isaac.z, -isaac.x);
```

The `velocity_commands` you feed the policy must be in the Isaac convention: `vx` forward,
`vy` to the robot's left, `wz` counter-clockwise seen from above.

`projected_gravity` is the world gravity direction expressed in the base frame, normalized - while
standing upright it is `(0, 0, -1)`. In Unity compute it as
`ToIsaac(Quaternion.Inverse(baseRotation) * Vector3.down)`.

Spawn pose: Isaac `[0.0, 0.0, 1.05]` = Unity `(0.000, 1.050, 0.000)`.

## Verifying the Unity port

`isaac_reference.json` is the contract, and it isolates two failure modes separately:

1. **Inference path** - feed each recorded `obs` through the ONNX model in Unity and compare with the
   recorded `action`. Should match to ~1e-6. Any mismatch is a Sentis/tensor-layout problem.
2. **Physics path** - replay the recorded actions in your Unity scene from the recorded spawn pose and
   compare `root_pos_w` / `joint_pos` drift over the 250 steps. Divergence here means
   gains, timestep, mass or friction differ.

The reference was recorded under a constant command `[1.0, 0.0, 0.0]` (vx, vy, wz).

## Measured policy performance

Isaac Lab, 64 robots, 30 s of simulated time, task's own random commands
(vx [0.0, 1.0], vy [0.0, 0.0],
wz [-1.0, 1.0], resampled every
[10.0, 10.0] s):

| Metric | Value |
|---|---|
| Mean linear-velocity tracking error | 0.117 m/s |
| Mean angular-velocity tracking error | 0.066 rad/s |
| Mean speed | 0.510 m/s |
| Falls | 4 (0.125 per robot per minute) |
