# Windows-side authoring venv (c:\Users\punko\Downloads\MujocoTest\.venv).
# Used for editing/inspecting the MJCF and replaying an exported policy in the
# native MuJoCo viewer. NO training happens here -- see requirements-wsl.txt.
#
#   python -m venv .venv
#   .\.venv\Scripts\python.exe -m pip install -r mjx_training\requirements-windows.txt

mujoco==3.12.0
numpy
